export interface ServicesProps {
  title: string;
  description: string;
  icon: React.ReactNode;
  className?: string;
}

export interface IServices {
  title: string;
  description: string;
  tag: string;
}
